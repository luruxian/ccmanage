#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 运行设计系统检查
echo "🔍 运行设计系统检查..."
npm run lint

# 如果lint失败，阻止提交
if [ $? -ne 0 ]; then
  echo "❌ 设计系统检查失败，请修复问题后再提交"
  exit 1
fi

echo "✅ 设计系统检查通过"